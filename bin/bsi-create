#!/usr/bin/env node
const chalk = require('chalk');
const { run, registerPlugins } = require('../commands/create');
run();

/**
 * const {run,registerPlugins } = require('../commands/create');
 * registerPlugins(function(creator){
 *
 * })
 */
